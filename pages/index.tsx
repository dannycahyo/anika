import { Typography } from "@mui/material";
import Layout from "@uikit/layout/Layout";

export default function Page() {
  return (
    <Layout>
      <Typography variant="body2">HELLO FROM HOME PAGE</Typography>
    </Layout>
  );
}
